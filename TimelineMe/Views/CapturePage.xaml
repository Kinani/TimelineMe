<Page
    x:Class="TimelineMe.Views.CapturePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimelineMe.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding MediaViewModelInstance, Source={StaticResource Locator}}"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="ms-appdata:///local/CCapture.jpg"></ImageBrush>
        </Grid.Background>
        <StackPanel Padding="10" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="OpenCameraApp" Foreground="Black" Command="{Binding OpenCamera}" Content="Can't wait to see you today! Click me?" Background="AliceBlue" Opacity="1"/>
        </StackPanel>
    </Grid>
</Page>

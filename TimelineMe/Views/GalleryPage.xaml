<Page
    x:Class="TimelineMe.Views.GalleryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimelineMe.Views"
    xmlns:conv="using:TimelineMe.Converters"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding GalleryPageViewModelInstance, Source={StaticResource Locator}}"
    mc:Ignorable="d">
    
    <Page.Resources>
        <conv:MediaNameConverter x:Key="MediaNameConverter"/>
        <conv:CaptureDateConverter x:Key="CaptureDateConverter"/>
        <conv:HighestEmoToTextConverter x:Key="HighestEmoToTextConverter"/>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <!--Paste Here-->
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="CapturedImagesGrid">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="Loaded">
                    <core:InvokeCommandAction Command="{x:Bind Vm.GalleryPageLoaded}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
            <ListView x:Name="CapturedImagesListView" ItemsSource="{Binding MediaCollection}" Header="Akona Batata" >
                <ListView.ItemTemplate>
                    <DataTemplate>

                        <StackPanel Padding="3" Orientation="Horizontal">
                            <Image Width="50" Height="50" Stretch="UniformToFill" Margin="0,0,5,0">
                                <Image.Source>
                                    <BitmapImage ImageFailed="BitmapImage_ImageFailed" ImageOpened="BitmapImage_ImageOpened" UriSource="{Binding MediaName,Converter = {StaticResource MediaNameConverter}}"></BitmapImage>
                                </Image.Source>
                            </Image>
                            <StackPanel Orientation="Vertical">
                                <TextBlock  FontSize="15" Text="{Binding CaptureDate}"></TextBlock>
                                <TextBlock FontFamily="13" Text="{Binding HighestEmotion, Converter={StaticResource HighestEmoToTextConverter}}"></TextBlock>
                            </StackPanel>
                        </StackPanel>
                        
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid x:Name="TimelineGrid" Grid.Row="1">
            <ListView x:Name="TimelineListView" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollMode="Enabled"
                      ScrollViewer.VerticalScrollMode="Disabled"
                      >
                <!--<interactivity:Interaction.Behaviors>
                    <core:EventTriggerBehavior EventName="ItemClick">
                        <core:InvokeCommandAction Command="{x:Bind Vm.ItemClicked}" CommandParameter="{Binding}"/>
                    </core:EventTriggerBehavior>
                </interactivity:Interaction.Behaviors>-->
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Background="Transparent" Orientation="Horizontal"></StackPanel>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
